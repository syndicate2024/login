# Add the new README and .gitignore changes
git add public/video/README.md .gitignore

# Commit the changes
git commit -m "Add video documentation and update .gitignore"

# Push to the dashboard branch
git push origin dashboard 